spring:
  application:
    name: eureka-server
server:
  port: 7777
logging:
  level:
    com.netflix.eureka: false
    com.netflix.discovery: false
    
    
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false